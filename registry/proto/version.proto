
syntax = "proto3";

package buffrs.version;

message VersionCore {
    uint32 major = 1;
    uint32 minor = 2;
    uint32 patch = 3;
}

message SemanticVersion {
    VersionCore version = 1;
    optional string prerelease = 2;
}
