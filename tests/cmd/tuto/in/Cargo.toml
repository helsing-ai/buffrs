[package]
name = "sensor-server"
version = "0.1.0"
edition = "2021"

[dependencies]
tonic = "0.11"
prost = "0.12"
prost-types = "0.12"

[build-dependencies]
tokio = { version = "1", features = ["full"] }
tonic-build = "0.11"
